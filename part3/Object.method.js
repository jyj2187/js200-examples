const obj = {
    movie: 'Sunny',
    music: 'Like Sugar',
    style: 'Retro',
    price: Infinity
};

//.keys(obj) 키만 추출
//.values(obj) 값만 추출
//.entries(obj) 배열로 변환
//.feeze(obj) 객체가 수정되지 않도록 동결
//.seal(obj) 객체에 속성을 추가 할 수 없게
//.assign(반환될 객체, obj1, obj2,....) 여러 객체들을 병합